"""
End-to-end voice chatbot pipeline.

Will define `VoiceChatbot`, which combines SpeechRecognizer,
IntentPredictor, and ResponseEngine to turn either raw audio or typed text
into a result dict containing transcript, intent, confidence, and response.
"""

# TODO (M3)
